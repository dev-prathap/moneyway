export interface VisitorData {
  visitorId?: string;
  type?: string;
  startDate?: string;
  endDate?: string;
  location?: string;
  // Add any other fields that might be needed
  passId?: string;
  eventId?: string;
  status?: string;
}
